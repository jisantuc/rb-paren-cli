name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.0.2
      - name: Setup Scala
        uses: olafurpg/setup-scala@v13
      - name: Run checks
        run: sbt ci-test
